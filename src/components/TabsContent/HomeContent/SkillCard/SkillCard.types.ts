// Interfaces and types from component SkillCard

// Component Props
export interface SkillCardProps {
  skill: string;
  principal?: boolean;
}

// Styled Component Props
export interface SkillCardStyledProps {
  className: string;
}
